apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
# Ressources Kubernetes "brutes" du monitoring (hors chart Helm kube-prometheus-stack,
# qui est déployé via Helm avec values-prometheus.yaml).
resources:
  - recording-rules.yaml
