# Image Airflow custom : base officielle 2.10.5 + nos DAGs FraudGuard.
# Les providers google et cncf.kubernetes sont déjà inclus dans l'image de référence.
FROM apache/airflow:2.10.5
COPY dags/ /opt/airflow/dags/
